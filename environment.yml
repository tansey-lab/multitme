name: multitme
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python>=3.12
  - pytorch>=2.0
  - numpy>=1.24
  - scanpy>=1.9
  - anndata>=0.10
  - omegaconf>=2.3
  - wandb>=0.16
  - pip
  - pip:
      - -e .
